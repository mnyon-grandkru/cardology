<% @cards.each do |card| %>
  <%= div_for card do %>
    <%= card.name %>
    <%= image_tag card.image_url, :class => 'image_gallery' %>
    <%= link_to 'edit', edit_card_path(card) %>
  <% end %>
<% end %>

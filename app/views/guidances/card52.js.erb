$('#delivery_second .<%=j frame_for @sequence %>').html("<%= j card_box_pane(@birthday, @card, 'planetary', @date, :here, @planet) %>")
var planetary_carousel = $('#delivery_second .<%=j frame_for @sequence %> .owl-carousel');
planetary_carousel.owlCarousel(owlOptions);
    
planetary_carousel.on('translated.owl.carousel', function(event) {
  cardReferenceToggler(event.target);
});

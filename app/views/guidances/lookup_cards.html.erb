<body class = "body">
  <div class="widget_heading">
    <section id="widget_title"><%= source_cards_marketing_text 'new_players', 'header1' %></section>
    <section id="widget_instructions">Enter Your Birthdate. Then Click Go.</section>
  </div>

  <%= form_for Birthday.new(:date => @date), :url => guidances_show_path(lookup_card: true, source: @source, show: true) do |birthday_form| %>
    <%= birthday_form.date_select :date, :start_year => 1900, :end_year => Time.now.year + 1, :order => [:month, :day, :year] %><br><br>
    <span class="reading_type_picker">
      <%= radio_button :birthday, :reading_type, :birth_card, :checked => true %>
      <%= label_tag 'birthday[reading_type]', 'Birth Card', :for => :birthday_reading_type_birth_card %>
    </span>
    <span class="reading_type_picker">
      <%= radio_button :birthday, :reading_type, :personality_card %>
      <%= label_tag 'birthday[reading_type]', 'Personality Card', :for => :birthday_reading_type_personality_card %>
    </span>
    <div onclick="startWobble()">
      <%= birthday_form.submit marketing_text('new_players', 'button1') %>
    </div>
  <% end %>
  <div id="guidance_zone" class= "guidance_prompt_media" >
    <div class="prompt_zone" >
      <%= image_tag (asset_path 'Low-Grey-Blue-RGB.jpg'), :class => 'source_cards_card_design' %>
    </div>
    <div class="prompt_zone" >
      <%= image_tag (asset_path 'Low-Grey-Blue-RGB.jpg'), :class => 'source_cards_card_design' %>
    </div>
    <div class="prompt_zone" >
      <%= image_tag (asset_path 'Low-Grey-Blue-RGB.jpg'), :class => 'source_cards_card_design' %>
    </div>
  </div>
</body>
<style>
  #guidance_prompt_media {
    justify-content: center;
  }
</style>
<script>
    function startWobble() {
    document.getElementById("guidance_zone").classList.add('wobble');
  }
</script>

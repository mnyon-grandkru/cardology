$('#delivery_third .<%=j daily_frame_for @year_sequence %>').html("<%= j card_box_pane(@birthday, @card, 'yearly', @date, :here) %>")
var year_carousel = $('#delivery_third .<%=j daily_frame_for @year_sequence %> .owl-carousel');
year_carousel.owlCarousel(owlOptions);
    
year_carousel.on('translated.owl.carousel', function(event) {
  cardReferenceToggler(event.target);
});

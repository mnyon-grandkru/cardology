<%= form_for Birthday.new, :remote => true do |birthday_form| %>
  <%= birthday_form.date_select :date, :start_year => 1900 %>
  <%= birthday_form.submit 'Check Reading' %>
<% end %>

<div id="results">
</div>
